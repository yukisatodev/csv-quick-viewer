# 設計判断について（DECISIONS）

このドキュメントは、READMEには書ききれない「なぜその技術・設計を選んだか」をまとめたものです。作った本人が、判断の理由と、そのトレードオフをどう認識しているかを共有する目的で書いています。

---

## 1. 処理をすべてブラウザ内で完結させた理由

CSVファイルをサーバーにアップロードせず、ブラウザ内（クライアントサイド）のみで読み込み・パース・表示まで完結させる設計にしています。

「中身を確認したいだけなのに、ファイルをどこかのサーバーに送信する」ことに抵抗を感じるユーザーは一定数いると考えました。特に業務で扱うCSVには、社外に出したくないデータが含まれることもあります。バックエンドを持たない構成にすることで、この懸念自体を設計上取り除いています。

副次的なメリットとして、サーバー費用が発生しない・GitHub Pagesのような静的ホスティングだけで公開が完結する、という運用面の利点もあります。

## 2. CSVパースにPapaParseを選んだ理由

CSVのパースは、見た目以上に考慮すべき点が多い処理です（カンマを含む値のクォート処理、改行コードの違い、文字エンコーディングなど）。自前でパーサーを書くよりも、実績のあるライブラリに任せることで、パース処理自体のバグに時間を使わず、UI/UXの実装に注力する判断をしました。

## 3. グラフ表示にRechartsを選んだ理由

数値列の傾向を素早く確認できる簡易グラフ機能を持たせるにあたり、Reactとの親和性が高く、宣言的にグラフを組めるRechartsを採用しました。凝ったカスタムビジュアライゼーションよりも、CSVの中身を素早く把握できることを優先する機能なので、シンプルに実装できることを重視しています。

## 4. テスト（Vitest）とCI（GitHub Actions）を導入した理由

型判定ロジック（数値・日付・文字列・空欄の自動判定）のように、入力と出力が明確に定義できる部分は、手動確認よりもテストコードで担保する方が信頼性が高いと判断しました。GitHub Actionsで、pushのたびに自動的にテストが走る構成にすることで、変更を加えるたびに手動で全機能を確認し直す手間を減らしています。

---

## 想定される質問への回答

**Q. なぜバックエンドを作らなかったのですか？**
このツールの価値は「手軽さ」にあると考えました。バックエンドを持たないことで、サーバー費用や認証の仕組みを考える必要がなくなり、実装をUI/UXの改善に集中させられます。また、ファイルを送信しないという設計自体が、プライバシー面での安心材料にもなっています。

**Q. 大きなCSVファイルでも快適に動きますか？**
現状は、ブラウザのメモリに収まる範囲のファイルサイズを想定した実装です。数十万行を超えるような大規模なファイルへの対応（仮想スクロールなど）は、今後の改善点として認識しています。
